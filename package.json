{
  "name": "sms-forwarder-tasker",
  "private": true,
  "scripts": {
    "lint": "npm run typecheck",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "build": "tsc --project tsconfig.json && node scripts/build.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "terser": "^5.37.0"
  }
}
